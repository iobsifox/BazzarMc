# BazzarMc — اتصال فروشگاه ووکامرس به سرور ماینکرافت

**توسعه‌دهنده:** Obsifox Studio

> اتصال فروشگاه **ووکامرس** به سرور **ماینکرافت** — نسخهٔ کاملاً ایرانی با ورود پیامکی، کد اتصال ۵ دقیقه‌ای، چک‌اوت اختصاصی فارسی/RTL و پنل مدیریت مدرن.

این پروژه یک **بازنویسی کامل** افزونهٔ `StoreLink for Minecraft` (نسخهٔ 1.0.36، GPL) است و دو بخش دارد:

| بخش | مسیر | فناوری |
|---|---|---|
| افزونهٔ وردپرس | `wp-plugin/bazzarmc/` | PHP 7.4+ / WordPress 6.0+ / WooCommerce 6.0+ |
| پلاگین ماینکرافت | `mc-plugin/` | Java 8+ / Spigot یا Paper 1.16 تا 1.21 |

خروجی نهایی:

* افزونهٔ وردپرس → پوشهٔ `wp-plugin/bazzarmc/` را zip کنید (یا مستقیم در `wp-content/plugins/` بگذارید).
* پلاگین ماینکرافت → `dist/BazzarMC-1.0.0.jar`

---

## ۱) امکانات

### سمت سایت (وردپرس)

* **سه روش تأیید هویت** — هرکدام به‌صورت مستقل از تب «ورود و عضویت» قابل فعال/غیرفعال‌کردن است و کاربر در فرم ورود روش دلخواه را انتخاب می‌کند:
  * **داشبورد وردپرس / پنل کاربری** — کد به‌جای پیامک، در همان صفحهٔ ورود (و ویجت پیشخوان مدیر) نمایش داده می‌شود و خودکار در جعبه‌های کد پر می‌شود؛ **بدون هیچ هزینهٔ پیامکی**.
  * **ایمیل** — کد به ایمیل ثبت‌شده در حساب کاربر فرستاده می‌شود.
  * **پیامک** — کد از راه کانال فعال (پل EZ-Login یا سرویس دلخواه شما) ارسال می‌شود.
* **ورود با رمز یک‌بارمصرف (OTP)** — کاربر شمارهٔ موبایل را وارد می‌کند، کد برایش ارسال می‌شود و بدون رمز عبور وارد سایت می‌شود (با ساخت خودکار حساب کاربری).
* **کانال پیامک منعطف** — ارسال از راه پل **EZ-Login** یا هر سرویس پیامکی دلخواه (کاوه‌نگار، SMS.ir، پیامیتو و…) فقط با پیاده‌سازی یک فیلتر: `bmc_sms_send_text`؛ به‌همراه **درایور ایمیل** به‌عنوان جایگزین/محیط تست.
* **هماهنگی کامل با EZ-Login (ایزی‌لاگین) بدون دست‌کاری آن افزونه** — کلیدهای متای موبایل (`billing_phone`, `phone`, `mobile`, `ez_phone`, …) از فیلتر `ez_login_phone_meta_keys` خوانده می‌شوند، آماده بودن سرویس پیامک آن تشخیص داده می‌شود و در صورت وجود، ارسال پیامک با تابع خودِ ایزی‌لاگین (`ez_send_sms()`) انجام می‌شود تا رفتار دو افزونه یکسان بماند. سرویس‌های پیامکی جدیدی هم که ایزی‌لاگین اضافه کند، به‌صورت خودکار دنبال می‌شوند.
* **سازگاری با افزونه‌های ورود پیامکی دیگر** — اگر *Digits*، *داروازه*، *WP-SMS* یا *پیامیتو* فعال باشند، افزونه آن‌ها را شناسایی می‌کند و شمارهٔ موبایل ذخیره‌شده توسط آن‌ها را می‌خواند/بازیابی می‌کند.
* **فارسی‌سازی کامل فیلدها** — برچسب و placeholder همهٔ فیلدهای ووکامرس (نام، نام خانوادگی، استان، شهر، آدرس، کد پستی، تلفن، ایمیل)، فهرست کامل استان‌های ایران، نام فارسی کشورها، قالب آدرس ایرانی و متن دکمه/کوپن/خطاها. تبدیل ارقام به فارسی هم اختیاری است.
* **استثنای خودکار از کش** — تعریف `DONOTCACHEPAGE`، هدرهای no-cache، فیلتر `rocket_cache_reject_uri` برای WP Rocket، کنترل LiteSpeed Cache، و در صورت واردکردن توکن کلادفلر: پاک‌سازی کش و ساخت/به‌روزرسانی **Cache Rule** از راه API. این کار از نشت کد یکبارمصرف یک کاربر به کاربر دیگر جلوگیری می‌کند.
* **بازپس‌گیری نقش‌ها** — با استرداد (refund) یا لغو سفارش، نقش‌های داده‌شده از کاربر گرفته می‌شوند (نقش‌های محافظت‌شده مثل مدیر و ویرایشگر هرگز گرفته نمی‌شوند).
* **رنک زمان‌دار** — برای هر محصول/نسخه مدت اعتبار نقش را به «روز» تعیین می‌کنید (`۰` = دائمی). زمان پایان هر اعطای نقش در متای کاربر ثبت می‌شود، زمان‌بند ساعتی نقش‌های منقضی را بازپس می‌گیرد و تمدید خرید، اعتبار را به زمان باقی‌مانده اضافه می‌کند.
* **کارت داشبورد حساب کاربری** — در صفحهٔ «حساب کاربری» ووکامرس، به‌جای متن پیش‌فرض، کارت اختصاصی BazzarMc نمایش داده می‌شود: نام واقعی کاربر (نام/نام خانوادگی)، ایمیل، موبایل و تعداد سفارش‌ها؛ جعبهٔ **اکانت ماینکرافت** (آواتار، نام کاربری، UUID، تاریخ اتصال و دکمهٔ «اتصال اکانت ماینکرافت» وقتی هنوز متصل نیست)؛ و فهرست **رنک‌های خریداری‌شده** با زمان باقی‌مانده، تاریخ انقضای شمسی و نوار پیشرفت. قابل خاموش‌کردن از تب «ورود».
* **آیکون‌های Material Symbols گوگل** — همهٔ ایموجی‌ها از رابط حذف شده و با آیکون‌های واقعی متریال (۹۷ آیکون، SVG درون‌خطی در `includes/class-bmc-icons.php` و `assets/js/bmc-icons.js`) جایگزین شده‌اند؛ بدون هیچ فونت یا درخواست خارجی.
* **کد اتصال ۵ دقیقه‌ای** در پنل کاربری: کاربر کد را کپی می‌کند و در بازی `/mclink <کد>` را می‌زند → حساب سایت به نام کاربری ماینکرافت متصل می‌شود.
* **سبد خرید اختصاصی** (`[bazzarmc_cart]`) — فهرست آیتم‌ها با تصویر و قیمت واحد، تغییر تعداد، حذف آیتم، کد تخفیف، جمع کل و **پرداخت در همان صفحه** (بدون رفرش و مراجعه به صفحهٔ دیگر).
* **گیت سبد خرید** — تا زمانی که اکانت ماینکرافت متصل نباشد، دکمهٔ «ادامهٔ فرآیند خرید / پرداخت» غیرفعال می‌ماند و به‌جای آن پیام «هنوز متصل نیستید» + دکمهٔ «اتصال اکانت ماینکرافت» نمایش داده می‌شود.
* **چک‌اوت اختصاصی** — کل فرم پرداخت ووکامرس با یک رابط فارسی/RTL و مدرن جایگزین می‌شود، درحالی‌که **سبد خرید، سفارش‌ها و درگاه‌های پرداخت (زرین‌پال و…) دست‌نخورده** باقی می‌مانند (موتور همان ووکامرس است).
* **پشتیبانی از چک‌اوت بلوکی ووکامرس** — سه حالت: اجبار به چک‌اوت کلاسیک (پیشنهادی)، تزریق فیلدهای ماینکرافت در چک‌اوت بلوکی، یا فقط هشدار به مدیر.
* **شناسهٔ تحویل محصول با ۵ حالت** — برای هر محصول (و هر نسخه/variation) می‌توانید تعیین کنید پلاگین ماینکرافت با کدام شناسه آن را پیدا کند: **نام محصول**، **شناسهٔ محصول (ID)**، **نامک (slug)**، **کد کالا (SKU)** یا **متن دلخواه**. مثال: محصولی با نام نمایشی «کوین ۱۰۰» و نامک `gold_pack_2` را می‌توانید با هر دو کلید تعریف کنید.
* **درگاه اجباری اتصال (Gate)** — کاربر متصل‌نشده اجازهٔ ورود به صفحهٔ خرید را ندارد و پیام «هنوز متصل نشده‌اید» + راهنمای اتصال را می‌بیند.
* **صف تحویل هوشمند** با چهار وضعیت (در انتظار / تحویل‌شده / ناموفق / منقضی)، نقش‌های کاربری به تفکیک محصول، امکان **هدیه‌دادن خرید** به یک بازیکن دیگر، و انقضا خودکار.
* **پنل مدیریت ۹ بخشی** با تقویم **شمسی**، آمار زنده، جست‌وجو، فیلتر، صفحه‌بندی، **خروجی CSV**، لاگ رویدادها و ابزارهای نگهداری.
* **مهاجرت از نسخهٔ قدیمی** — انتقال نام‌های کاربری متصل، صف تحویل و تنظیمات از `storelinkformc`.
* **سازگاری معکوس (Legacy)** — مسیرهای REST افزونهٔ قدیمی نیز به‌صورت اختیاری پاسخ داده می‌شوند.

### سمت ماینکرافت

* **نام دستور و نام‌های جایگزین کاملاً قابل تغییر توسط مدیر** — کلیدهای `command.name`، `command.aliases`، `command.admin-name` و `command.admin-aliases` در `config.yml`؛ دستورهای سفارشی در زمان اجرا ثبت می‌شوند (بدون نیاز به ویرایش `plugin.yml`).
* دستور `/mclink <کد>` برای استفاده از کد پنل کاربری.
* دستور `/mclink phone <شماره>` و `/mclink phone <شماره> <کد>` برای اتصال با رمز پیامکی از داخل بازی (و `/mclink mail …` برای ایمیل).
* `/mclink status` و `/mclink claim`.
* **تحویل خودکار**: هر ۴۵ ثانیه (قابل تنظیم) برای بازیکنان آنلاین و همچنین هنگام ورود به سرور.
* تعریف محصولات در `config.yml` به‌صورت **آیتم** (material/amount/name/lore)، **دستور کنسول** (مثل LuckPerms/EssentialsX) یا ترکیب هر دو — و حتی شکل کوتاه `"gold_pack_2": "eco give {player} 100"`.
* **تطبیق محصول با ۵ کلید** به همین ترتیب: شناسهٔ تحویل تعیین‌شده در پنل ← نام محصول ← شناسهٔ محصول/نسخه ← نامک ← SKU.
* **کلید `api.verify-ssl`** — برای سایت‌هایی با گواهی خودامضا می‌توان اعتبارسنجی SSL را خاموش کرد (هماهنگ با `ssl_verify` در پنل وردپرس).
* **متغیرهای فروشگاه و سرور** در همهٔ پیام‌ها و دستورها: `{store}` `{store_url}` `{ip}` `{server}` `{cmd}` `{admin_cmd}` `{player}` `{uuid}` `{key}` `{slug}` `{sku}`.
* همهٔ درخواست‌های شبکه **ناهمگام** (Async) — بدون لگ روی سرور.
* خروجی با بایت‌کد Java 8 → روی سرورهای Java 8 تا 21 کار می‌کند.

### تنظیم‌های کلیدی نسخهٔ ۱.۰

| تب | کلید/گزینه | کار |
|---|---|---|
| ورود و عضویت | **روش‌های تأیید هویت** | فعال/غیرفعال‌کردن هر یک از سه روش: داشبورد، ایمیل، پیامک + تعیین روش پیش‌فرض |
| ورود و عضویت | ویجت پیشخوان مدیر / جعبهٔ کد در پنل کاربری | نمایش کد روش «داشبورد» برای مدیر و برای خود کاربر |
| ورود و عضویت | صفحهٔ سبد خرید / صفحهٔ پرداخت | انتخاب برگهٔ `[bazzarmc_cart]` و `[bazzarmc_checkout]` |
| ورود و عضویت | کارت حساب کاربری | نمایش کارت BazzarMc در داشبورد «حساب کاربری» ووکامرس |
| پیامک | کانال ارسال | پل EZ-Login / فیلتر `bmc_sms_send_text` / ایمیل / غیرفعال |
| پیامک | هماهنگی با EZ-Login | وضعیت افزونه، کلیدهای موبایل شناسایی‌شده، خواندن تنظیمات و همگام‌سازی شماره |
| محصولات | شناسهٔ پیش‌فرض + شناسهٔ هر محصول | نام / شناسهٔ محصول / نامک / SKU / متن دلخواه |
| محصولات | اعتبار پیش‌فرض رنک + اعتبار هر محصول | مدت فعال ماندن نقش پس از خرید (روز؛ `۰` = دائمی) |
| محصولات | بازپس‌گیری نقش‌ها | گرفتن نقش‌ها هنگام استرداد یا لغو سفارش |
| چک‌اوت | صفحهٔ سبد خرید + گیت دکمهٔ پرداخت | سبد اختصاصی و غیرفعال‌ماندن دکمهٔ پرداخت تا اتصال اکانت |
| چک‌اوت | فارسی‌سازی فیلدها / نام کشورها / ارقام فارسی | ترجمهٔ کامل برچسب‌ها و placeholderها |
| اتصال سرور | اعتبارسنجی گواهی SSL | هماهنگ با `api.verify-ssl` در پلاگین ماینکرافت |
| اتصال سرور | بررسی نام کاربری مویانگ | اعتبارسنجی نام کاربری با سرویس مویانگ |
| اتصال سرور | چک‌اوت بلوکی | اجبار به کلاسیک / تزریق فیلدها / فقط هشدار |
| اتصال سرور | استثنا از کش + توکن و Zone کلادفلر | جلوگیری از کش‌شدن صفحه‌های حساس و ساخت قاعدهٔ کش |
| ابزارها | پاک‌سازی کش / ساخت قاعدهٔ کلادفلر | اجرای دستی |
| متن‌ها | متن مسدودسازی سبد / متن صفحهٔ تشکر | با متغیرهای `{connect_url}` `{player}` `{server}` `{order}` `{site}` |

---

## ۲) نصب و راه‌اندازی

### گام ۱ — نصب افزونهٔ وردپرس

1. پوشهٔ `bazzarmc` را در `wp-content/plugins/` قرار دهید و فعال کنید.
2. جدول‌های دیتابیس خودکار ساخته می‌شوند:
   * `{prefix}bmc_deliveries` — صف تحویل
   * `{prefix}bmc_codes` — کدهای اتصال و رمزهای یک‌بارمصرف (هش‌شده)
   * `{prefix}bmc_logs` — لاگ رویدادها

### گام ۲ — پیکربندی پنل (پیشخوان ← BazzarMc)

| تب | کار اصلی |
|---|---|
| **داشبورد** | آمار زنده، آخرین رویدادها، سلامت سیستم |
| **اتصال سرور** | آدرس/نام سرور، **توکن API**، سیاست نام کاربری، روش‌های اتصال مجاز، مدت اعتبار کد (پیش‌فرض ۵ دقیقه)، نقش پیش‌فرض کاربران متصل |
| **محصولات** | انتخاب محصولاتی که تحویل داده می‌شوند + نقش کاربری، شناسهٔ تحویل و **مدت اعتبار نقش** هر محصول |
| **صف تحویل‌ها** | مشاهده/فیلتر/جست‌وجو، تحویل دستی، بازگرداندن به صف، حذف، **خروجی CSV** |
| **پیامک** | انتخاب کانال ارسال، وضعیت پل EZ-Login، قوانین ارسال، متن‌ها، درایور ایمیل و ارسال پیامک آزمایشی |
| **ورود** | ورود پیامکی، ساخت خودکار حساب، همگام‌سازی با ایزی‌لاگین/Digits، کلیدهای متای موبایل، برگهٔ ورود |
| **چک‌اوت** | حالت اختصاصی/پیش‌فرض، درگاه اجباری اتصال، پوسته و رنگ، فونت، فیلدهای فرم، کد هدیه |
| **پیام‌ها** | همهٔ متن‌های رابط کاربری + شورت‌کدهای قابل استفاده |
| **ابزارها** | ساخت برگه‌ها، بازسازی جدول‌ها، اجرای زمان‌بند، پاک‌کردن لاگ، مهاجرت از نسخهٔ قدیمی، اسکن شمارهٔ موبایل، حذف داده‌ها هنگام حذف افزونه |

### گام ۳ — کانال پیامک

در تب **پیامک** یکی از این سه کانال را انتخاب کنید:

| کانال | چه زمانی |
|---|---|
| **پل EZ-Login** (پیش‌فرض) | اگر افزونهٔ EZ-Login فعال و سرویس پیامک آن آماده باشد؛ ارسال با تابع خودِ ایزی‌لاگین (`ez_send_sms()`) انجام می‌شود و تنظیمات/الگوها از همان افزونه خوانده می‌شود. |
| **فیلتر اختصاصی** | برای هر سرویس پیامکی دیگر؛ کافی است فیلتر زیر را در قالب یا یک افزونهٔ کوچک پیاده‌سازی کنید. |
| **ایمیل** | محیط تست یا سایت‌های بدون پنل پیامکی — کد به ایمیل کاربر فرستاده می‌شود. |

```php
add_filter( 'bmc_sms_send_text', function ( $result, $to, $text, $code, $force_text ) {
	$response = my_sms_provider_send( $to, $text );

	if ( is_wp_error( $response ) ) {
		return $response;
	}

	return array(
		'success' => (bool) $response['ok'],
		'message' => (string) $response['message'],
	);
}, 10, 5 );
```

مقادیر مجاز بازگشتی: `true`/`false`، نمونهٔ `WP_Error` یا آرایهٔ `array( 'success' => bool, 'message' => string )`. بازگرداندن `null` یعنی «این فیلتر مسئولیتی ندارد» و افزونه به کانال بعدی (پل EZ-Login) می‌رود. هر ارسال و خطا در تب **ابزارها** ثبت می‌شود.

سپس روی **ارسال پیامک آزمایشی** بزنید تا مسیر انتخابی با شمارهٔ دلخواه آزموده شود.

> نکته: برای ارسال کد یکبارمصرف، خط **خدماتی** یا **الگوی تأییدشده** لازم است؛ با خطوط تبلیغاتی، پیامک به کاربران دارای «دریافت پیامک تبلیغاتی ممنوع» نمی‌رسد.

### گام ۴ — ساخت برگه‌ها

در تب **ابزارها** روی «ساخت برگه‌ها» بزنید تا سه برگه ساخته شود، یا شورت‌کدها را دستی در برگه‌های دلخواه بگذارید:

```text
[bazzarmc]            پنل اتصال کاربر (وضعیت + کد ۵ دقیقه‌ای)
[bazzarmc_login]      فرم ورود با موبایل
[bazzarmc_status]     خلاصهٔ وضعیت
[bazzarmc_checkout]   صفحهٔ پرداخت اختصاصی
```

اندپوینت «حساب کاربری من → ماینکرافت» هم خودکار ثبت می‌شود: `/my-account/minecraft/`.

### گام ۵ — نصب پلاگین ماینکرافت

```bash
# ۱) فایل jar را در پوشهٔ plugins سرور بگذارید
cp dist/BazzarMC-1.0.0.jar /path/to/server/plugins/

# ۲) سرور را یک‌بار ری‌استارت کنید تا config.yml ساخته شود
# ۳) فایل plugins/BazzarMC/config.yml را ویرایش کنید:
#      api.base-url : "https://yoursite.com"
#      api.token    : <توکن از تب «اتصال سرور»>
# ۴) محصولات را در بخش products تعریف کنید
# ۵) در کنسول: bmc reload  و سپس  bmc ping
```

تست نهایی در بازی:

```text
/mclink status          → وضعیت اتصال
/mclink ABC123          → استفاده از کد پنل کاربری
/mclink phone 09123456789   → دریافت کد پیامکی
/mclink phone 09123456789 12345 → تأیید و اتصال
/mclink claim           → دریافت آیتم‌های در انتظار
```

---

## ۳) جریان کار (از خرید تا تحویل)

```text
کاربر              سایت (وردپرس)                        سرور ماینکرافت
 │                     │                                        │
 │  ۱. ورود با موبایل  │                                        │
 ├────────────────────►│  ارسال OTP (کانال فعال)                │
 │◄────────────────────┤                                        │
 │  ۲. پنل → کد اتصال  │                                        │
 ├────────────────────►│  ساخت کد ۵ دقیقه‌ای (هش‌شده)            │
 │◄────────────────────┤                                        │
 │  ۳. /mclink <کد>    ├───────────────────────────────────────►│
 │                     │  POST /link/redeem  (X-BMC-Token)     │
 │                     │◄───────────────────────────────────────┤
 │                     │  اتصال user ↔ minecraft_player + uuid  │
 │  ۴. خرید (چک‌اوت اختصاصی)                                     │
 ├────────────────────►│  gate_status() = linked (OK)           │
 │                     │  پرداخت از طریق درگاه (زرین‌پال و…)     │
 │                     │  wc order → completed/processing       │
 │                     │  ایجاد ردیف در صف تحویل                │
 │                     │◄──────── GET /deliveries?player=… ─────┤
 │                     ├──── آیتم‌ها/دستورها ──────────────────►│
 │                     │◄──── POST /deliveries/mark ────────────┤
 │                     │  وضعیت → تحویل‌شده (+ پیامک اطلاع)      │
```

---

## ۴) ساختار فایل‌ها

```text
bazzarmc/
├── wp-plugin/bazzarmc/          ← افزونهٔ وردپرس
│   ├── bazzarmc.php             نقطهٔ ورود، ثابت‌ها، بارگذاری کلاس‌ها
│   ├── uninstall.php                حذف داده‌ها (مشروط به تنظیمات)
│   ├── readme.txt
│   ├── includes/
│   │   ├── class-bmc-helpers.php   ابزارها (nonce، پاک‌سازی، مقایسهٔ امن)
│   │   ├── class-bmc-jalali.php    تقویم شمسی
│   │   ├── class-bmc-logger.php    لاگ رویدادها
│   │   ├── class-bmc-settings.php  همهٔ تنظیمات (یک option آرایه‌ای)
│   │   ├── class-bmc-db.php        ساخت/ارتقای جدول‌ها
│   │   ├── class-bmc-link.php      کد اتصال، OTP اتصال، نام کاربری MC
│   │   ├── class-bmc-sms.php       لایهٔ ارسال پیامک/ایمیل
│   │   ├── class-bmc-icons.php     مجموعهٔ آیکون Material Symbols (SVG درون‌خطی)
│   │   ├── class-bmc-roles.php     اعطا/تمدید/انقضای رنک‌های زمان‌دار
│   │   ├── class-bmc-verify.php    سه کانال تأیید هویت
│   │   ├── sms/class-bmc-sms-driver.php      قرارداد درایور
│   │   ├── sms/class-bmc-sms-ezlogin.php     پل EZ-Login + فیلتر bmc_sms_send_text
│   │   ├── sms/class-bmc-sms-email.php       درایور ایمیل
│   │   ├── integrations/class-bmc-ezlogin.php سازگاری با ایزی‌لاگین/Digits
│   │   ├── class-bmc-api.php       مسیرهای REST (+ سازگاری نسخهٔ قدیمی)
│   │   ├── class-bmc-deliveries.php صف تحویل
│   │   ├── class-bmc-cron.php      زمان‌بند نگهداری ساعتی
│   │   ├── class-bmc-auth.php      ورود OTP سایت + سازگاری با ایزی‌لاگین/Digits
│   │   ├── class-bmc-checkout.php  چک‌اوت اختصاصی + درگاه اتصال
│   │   ├── class-bmc-public.php    شورت‌کدها، اندپوینت حساب کاربری، دارایی‌ها
│   │   ├── class-bmc-migrate.php   مهاجرت از نسخهٔ قدیمی، ساخت برگه‌ها، اسکن موبایل
│   │   └── class-bmc-admin.php     منو، تب‌ها، AJAX، خروجی CSV
│   ├── admin/views/*.php            ۹ نمای پنل مدیریت
│   ├── public/templates/*.php       link-box, gate, cart, checkout, login, wc-dashboard
│   └── assets/{css,js}/             استایل، اسکریپت‌ها و نقشهٔ آیکون‌ها
├── mc-plugin/                       ← پلاگین ماینکرافت (Java)
│   ├── pom.xml
│   ├── README.md
│   └── src/main/{java,resources}/
├── dist/BazzarMC-1.0.0.jar       ← خروجی آمادهٔ نصب
└── docs/{README-fa.md, API.md}
```

---

## ۵) ساخت پلاگین ماینکرافت از سورس

پیش‌نیاز: **JDK 17** (یا 11) و **Maven 3.6+**. بایت‌کد خروجی Java 8 است، پس روی همهٔ سرورها کار می‌کند.

```bash
cd mc-plugin
mvn clean package
# خروجی: target/BazzarMC-1.0.0.jar
```

اگر Maven ندارید:

```bash
# دانلود Maven
curl -LO https://dlcdn.apache.org/maven/maven-3/3.9.16/binaries/apache-maven-3.9.16-bin.tar.gz
tar xzf apache-maven-3.9.16-bin.tar.gz

# دانلود JDK 17 (Temurin)
curl -LO https://github.com/adoptium/temurin17-binaries/releases/download/jdk-17.0.20.1%2B1/OpenJDK17U-jdk_x64_linux_hotspot_17.0.20.1_1.tar.gz
tar xzf OpenJDK17U-jdk_x64_linux_hotspot_17.0.20.1_1.tar.gz

JAVA_HOME=$PWD/jdk-17.0.20.1+1 $PWD/apache-maven-3.9.16/bin/mvn -f mc-plugin/pom.xml clean package
```

وابستگی‌ها فقط `provided` هستند (spigot-api و gson) → jar نهایی **بدون نیاز به shade** و سبک است.

---

## ۶) نکات امنیتی

* توکن API به‌صورت تصادفی (۴۰ کاراکتر هگز) ساخته می‌شود و با `hash_equals` مقایسه می‌گردد؛ در تب «اتصال سرور» قابل بازنشانی است.
* کدهای اتصال و رمزهای یک‌بارمصرف با `hash_hmac('sha256', …, wp_salt('auth'))` ذخیره می‌شوند؛ متن اصلی فقط در یک transient کوتاه‌مدت نگه داشته می‌شود تا پس از رفرش هم به کاربر نمایش داده شود.
* محدودیت نرخ (Rate limit) برای درخواست کد، تأیید کد و redeem.
* همهٔ فرم‌ها با nonce وردپرس و بررسی سطح دسترسی (`manage_woocommerce`) محافظت می‌شوند.
* پاسخ‌های REST با هدرهای `no-store` ارسال می‌شوند تا توسط LiteSpeed/WP Rocket/Cloudflare کش نشوند.
* برای HTTPS اجباری، `api.base-url` را با `https://` وارد کنید.

---

## ۷) عیب‌یابی

| مشکل | راه‌حل |
|---|---|
| در بازی «ارتباط با سایت برقرار نشد» | `bmc ping` را در کنسول بزنید؛ آدرس سایت، توکن و فعال‌بودن REST را بررسی کنید. تب **ابزارها** ← «تست اتصال» هم همین کار را می‌کند. |
| پیامک ارسال نمی‌شود | تب **پیامک** ← «ارسال پیامک آزمایشی»؛ وضعیت کانال انتخابی (آماده بودن EZ-Login یا فیلتر `bmc_sms_send_text`) را بررسی کنید و پیام خطای ثبت‌شده در تب **ابزارها** ← لاگ رویدادها را ببینید. |
| کاربر موبایل ندارد | تب **ورود** ← «شناسایی کلیدها» و سپس «همگام‌سازی شماره‌ها» را اجرا کنید تا شمارهٔ موبایل از متاهای دیگر (billing_phone، digits_number و…) به `bmc_mobile` منتقل شود. |
| آیتم تحویل داده نمی‌شود | محصول باید در تب **محصولات** انتخاب شده باشد و در `config.yml` پلاگین ماینکرافت هم بخش `products` کلید متناظر (نام محصول با حروف کوچک یا شناسهٔ محصول) وجود داشته باشد. |
| چک‌اوت اختصاصی نمایش داده نمی‌شود | تب **چک‌اوت** ← حالت = «اختصاصی»؛ مطمئن شوید برگهٔ پرداخت ووکامرس همان برگهٔ استاندارد است و شورت‌کد `[woocommerce_checkout]` حذف نشده باشد (افزونه خودش جایگزین می‌کند). |
| صفحهٔ خرید مسدود است | این رفتار «درگاه اجباری اتصال» است؛ از تب **چک‌اوت** غیرفعالش کنید یا کاربر را متصل کنید. |
| رنک کاربر پس از پایان اعتبار حذف نشد | زمان‌بند ساعتی (`bmc_hourly_maintenance`) باید اجرا شود — تب **ابزارها** ← «اجرای زمان‌بند». نقش‌های محافظت‌شده هرگز گرفته نمی‌شوند. |
| کارت داشبورد حساب کاربری دیده نمی‌شود | تب **ورود** ← بخش «پنل حساب کاربری» باید روشن باشد؛ اگر قالب شما `myaccount/dashboard.php` را override کرده، قالبِ child اولویت بیشتری نسبت به فیلتر افزونه (اولویت ۲۰) دارد. |

برای گزارش دقیق‌تر، تب **اتصال سرور** ← «حالت دیباگ» را روشن کنید و لاگ‌ها را در تب **ابزارها** ببینید.

---

## ۸) پروانه

GPL-2.0-or-later — همانند افزونهٔ اصلی که این پروژه از آن الهام گرفته شده است.
